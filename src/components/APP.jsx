import React from 'react';
import Main from './Main';

const APP = ({ history }) => {
    return (
        <div>
            2234
            <Main history={history} />
        </div>
    );
};

export default APP;